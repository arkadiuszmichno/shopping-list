extends layout

block content
    if rows.length
        table.listing-table.table-dark.table-striped
            tr
                th Name
                th Email
            each user in rows
                tr
                    td= user.name
                    td= user.email
    else
        p No users yet :(